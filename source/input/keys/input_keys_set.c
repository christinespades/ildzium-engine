#include "pch.h"
#include "input_keys_set.h"

/*
set_param_enum(
    PARAM_INPUT_BIND_CAMERA_MOVE_LEFT,
    KEY_F
);

platform_key get_input_binding(MacroParamID id)
{
    ParamEntry* p = &s_macro_param_registry[id];

    assert(p->enum_type == ENUM_TYPE_PLATFORM_KEY);

    return (platform_key)p->value.e;
}*/