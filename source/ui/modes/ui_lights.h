#pragma once
#include "ui/ui.h"

void setup_lights_controls(UI_Context* ctx);

// this will visualize light sources, let you select and move points, attach lights to meshes
// change color, attenutation, intensity
