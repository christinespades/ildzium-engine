#pragma once
#include "core/parser_md.h"

/*
void ui_add_glyph(UI_Context* ctx, int x, int y, char c,
                  float scale, bool bold, bool italic);
void ui_add_text_link(UI_Context* ctx,
                      int x, int y,
                      const char* text, int len,
                      const char* url, int url_len);
void render_markdown(UI_Context* ctx, int x, int y, int w, int h, const char* text);*/